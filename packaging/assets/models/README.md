# Bundled faster-whisper models

Place local CTranslate2/faster-whisper model folders here before building a packaged app.

Expected layout:

```text
packaging/assets/models/small/
packaging/assets/models/medium/
packaging/assets/models/large-v3/
```

Each model folder should contain the model files used by `faster-whisper`.


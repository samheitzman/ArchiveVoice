# Bundled ffmpeg binaries

Place platform-specific binaries in this folder before building a packaged app.

macOS:

- `ffmpeg`
- `ffprobe`

Windows:

- `ffmpeg.exe`
- `ffprobe.exe`

Archive Voice will prefer these bundled binaries when present.

